# This workflow fetches GitHub Actions data from bitcoin/bitcoin and updates the stats
name: bitcoin-bitcoin-github-actions

on:
  schedule:
    - cron: '15 0,3,6,9,12,15,18,21 * * *'  # Run every 3 hours
  workflow_dispatch:  # Allow manual triggering

concurrency:
  group: "fetch-github-actions"
  cancel-in-progress: false

jobs:
  fetch-github-actions:
    runs-on: ubuntu-latest
    env:
      START_RUN_ID: 17430440869
    steps:
    - uses: actions/checkout@v5
      with:
        fetch-depth: 0
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Install Nix
      uses: DeterminateSystems/nix-installer-action@main
      with:
        determinate: true
        extra-conf: lazy-trees = true

    - name: Set Up Cachix
      uses: cachix/cachix-action@v15
      with:
        name: bitcoin-core-ci-stats
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}

    - name: Build binary
      run: |
        nix build
        cp scripts/data_shards.py "$RUNNER_TEMP/data_shards.py"

    - name: Configure git and checkout data branch
      run: |
        git config --local user.email "willcl-ark+github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git checkout bitcoin-bitcoin-tasks

    - name: Assemble existing tasks
      run: |
        if [ -f tasks.json.gz ]; then
          gunzip tasks.json.gz
        else
          python3 "$RUNNER_TEMP/data_shards.py" assemble data/tasks tasks.json
        fi

    - name: Fetch GitHub Actions data (with checkpoint)
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        # If checkpoint doesn't exist, start from run ID where Ci changes were merged
        if [ ! -f .checkpoint.json ]; then
          echo "No checkpoint found, starting from run ID ${{ env.START_RUN_ID }}"
          ./result/bin/fetch-tasks-github --runs 4500 --owner bitcoin --repository bitcoin --since-run-id ${{ env.START_RUN_ID }} --checkpoint
        else
          echo "Checkpoint found, using incremental fetch"
          ./result/bin/fetch-tasks-github --runs 4500 --owner bitcoin --repository bitcoin --checkpoint
        fi

    - name: Pack monthly data
      run: |
        python3 "$RUNNER_TEMP/data_shards.py" pack tasks.json data/tasks creationTimestamp
        python3 "$RUNNER_TEMP/data_shards.py" pack graph.json data/graph created
        rm tasks.json graph.json

    - name: Commit and push changes
      run: |
        git rm -f --ignore-unmatch tasks.json.gz graph.json
        git add -A -- data stats.json test-timings.json pr-test-timings .checkpoint.json
        if git diff --staged --quiet; then
          echo "No changes to commit"
        else
          git commit -m "Auto updated GitHub Actions stats: $(date)"
          git push origin bitcoin-bitcoin-tasks
        fi
